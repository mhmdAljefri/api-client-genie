# API Client Genie :magic:

An Api generator tool that convert endpoint into Typed http Client function

It's currently in beta.

```
npx api-client-genie --token=<token> --endpoint=<http-endpoint>
```